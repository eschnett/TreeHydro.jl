# Generated by TREEHYDRO_TEST_LONG=1 TREEHYDRO_REGENERATE=1
# julia --project=. -e 'using Pkg; Pkg.test()'.
#
# Reduced configurations of the physics studies and the
# outputs the short tier compares against, to roundoff
# (rtol = 1e-12). Committed and reviewed like code: a
# changed number here is a changed numerical result, and
# the commit that changes it says why. Each table's
# `_config` sub-table records the configuration it came
# from. See test/references.jl.

[ramp_tau_h64]
h = 0.015625
tau_max = 0.2535357035186758

    [ramp_tau_h64._config]
    D = 1
    N = 16
    fixup = true
    limiter = "minmod"
    p = 3
    refined = "false"
    riemann = "hlle"
    roots = [4]
    t_end = "0"

[sod_tau_h64]
h = 0.015625
tau_contact = 0.1871204148672731
tau_fan = 0.09009607151354032
tau_head = 0.173573994669743
tau_shock = 0.5413040054754896
tau_tail = 0.33005360025767094

    [sod_tau_h64._config]
    D = 1
    N = 16
    fixup = true
    limiter = "minmod"
    p = 3
    refined = "false"
    riemann = "hlle"
    roots = [4]
    t_end = "1//10"
